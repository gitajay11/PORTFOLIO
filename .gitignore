node_modules
.next
out
.DS_Store
*.tsbuildinfo
next-env.d.ts

# local env — never commit API keys
.env
.env*.local

# source certificate PDFs — personal documents, not needed at runtime.
# Everything the site uses from them (title, issuer, date, verify link) is
# already extracted into lib/content.ts.
/certificates
